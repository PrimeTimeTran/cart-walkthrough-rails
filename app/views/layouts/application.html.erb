<!DOCTYPE html>
<html>

<head>
  <title>Kathy's Kitchen</title>
  <link rel="icon" href="http://coolspotters.com/files/photos/26546/ruby-on-rails-profile.png">
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body class="<%= controller_name %>">
  <nav class="navbar navbar-toggleable navbar-light bg-faded">
    <a class="navbar-brand" href="<%= root_path %>">Kathys Kitchen</a>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item <%= active_class('/menu') %>">
          <a class="nav-link" href="<%= menu_path %>"><%= fa_icon "cutlery" %>  Menu</a>
        </li>  
        <li class="nav-item <%= active_class('/contact_us') %>">
          <a class="nav-link" href="<%= contact_us_path %>"><%= fa_icon "phone" %>  Contact Us</a>
        </li>

      </ul>
      <ul class="navbar-nav pull-right">
        <li class="nav-item <%= active_class('/cart') %>">
          <a class="nav-link" href="<%= cart_path %>"><%= fa_icon "shopping-cart" %>  Cart(<%= get_cart.item_count %>) <span style="color:green"><%= number_to_currency(get_cart.total) %></span></a>            
        </li>
      </ul>
    </div>
  </nav>
  <div class="container ">  
    <%= yield %>
  </div>
</body>

</html>